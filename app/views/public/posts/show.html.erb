<h2>投稿詳細</h2>
<%= image_tag @post.image, size: "100x100" %>
<%= @post.title %>
<%= @post.description %>
<%= @post.created_at %>
<% @tags.each do |tag| %>
  <%= link_to tag.name, search_tag_path(tag_id: tag.id) %>
<% end %>